Drop these two files into this folder before publishing:

- jacob.Photo.jpg  → portrait used in the Section 1 hero
- jacobLogo.png    → logo shown in the top-left button

Git does not track empty folders, so this file also keeps
the `img/` directory present in the repository until then.
